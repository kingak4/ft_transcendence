services:
  frontend:
    build: .
    container_name: transcend-frontend
    env_file:
      - .env.dev
    ports:
      - "3000:3000"

networks:
  default:
    external: true
    name: postgres_app_network