/**
 * The Shared module contains common utilities, DTOs, interfaces, and base configuration that can be
 * reused across all other modules in the application.
 */
@ApplicationModule(displayName = "Shared", type = ApplicationModule.Type.OPEN)
package code.shared;

import org.springframework.modulith.ApplicationModule;
