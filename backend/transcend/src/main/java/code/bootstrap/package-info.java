/**
 * The Bootstrap module contains all the application entry point logic, global configuration,
 * and base exception handlers to bootstrap the Transcend application.
 */
@ApplicationModule(displayName = "Bootstrap")
package code.bootstrap;

import org.springframework.modulith.ApplicationModule;
