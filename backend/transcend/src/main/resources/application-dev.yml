spring:
  application:
    name: transcend-backend-dev
  liquibase:
    enabled: true
    change-log: classpath:changelog/db.changelog-master.yaml
    default_schema: dev
    parameters:
      schemaName: dev
  jpa:
    defer-datasource-initialization: false
    properties:
      hibernate:
        default_schema: dev
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:postgresql://postgres-db-container:5432/${POSTGRES_DB}?sslmode=disable&currentSchema=dev
server:
  port: 5000