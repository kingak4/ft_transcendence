services:
  transcend-backend:
    ports:
      - "127.0.0.1:5001:5000"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
  transcend-backend-test:
    environment:
      - SPRING_PROFILES_ACTIVE=test,dev

networks:
  default:
    external: true
    name: transcend-net
